#include<bits/stdc++.h>
using namespace std;
void Print (se<string> &s){
    for(string value : s)
}
 
int main(){
    set <string> s;
    s.insert("abc");    //log(n)
    s.insert("zsdf");    
    s.insert("bcd"); 
    auto it = s.find("abc");  


 
return 0;
 
}